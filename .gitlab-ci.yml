stages:
  - build
  - test
  - deploy
  
cache:
  paths:
    - target/
  
build:
  stage: build
  tags:
    - maven
  script:
    - mvn clean package
  artifacts:
    paths:
      - target/transportpipes-**.jar
deploy:
  stage: deploy
  tags:
    - maven
  script:
    - mvn deploy
